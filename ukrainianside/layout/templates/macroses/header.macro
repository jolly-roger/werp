{% macro header(aticleAlias) -%}
    <h3>
        <a href="{{ getUrlByAlias(aticleAlias) }}"
           title="Постоянная ссылка на {{getTitleByAlias(aticleAlias)}}" 
           rel="bookmark">{{getTitleByAlias(aticleAlias)}}</a></h3>
{%- endmacro %}